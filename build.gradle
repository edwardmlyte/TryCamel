// Top-level build file. Configuration common to all sub-projects/modules
// lives here. Plugins are declared (apply false) so module scripts can apply
// them without repeating versions; versions come from gradle/libs.versions.toml.
plugins {
    alias(libs.plugins.android.application) apply false
}
